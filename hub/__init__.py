"""MCP Hub - one local port, several MCP servers, full call telemetry.

The whole application is standard-library Python plus one downloaded Caddy
binary. Nothing is vendored into the repository, so a release stays a few
hundred kilobytes.
"""

__version__ = "3.4.1"
